// Intentionally empty.
//
// The Roster aggregate now lives in StaffRoster.cs (the type is named `StaffRoster` because
// CON-009 declares a query property named `Roster`, and C# forbids a member sharing its enclosing
// type's name). This file was superseded during TKT-012; it is retained only because the
// sandbox in this headless session blocked file deletion/rename under `.claude/worktrees`.
// Safe to `git rm` at commit time.

namespace TavernIdler.Domains.Staffing;
