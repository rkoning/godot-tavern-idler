# TKT-017: RNG adapter (CON-015 implementation)

> Status: DONE
> Type: implementation
> Domain: adapters (shared)
> Traces to: REQ-024, REQ-050, REQ-102, REQ-110 (deterministic draws)
> Blocked by: TKT-001 | Blocks: TKT-022, TKT-026
> Session: /implement TKT-017 (2026-07-13)

## Goal

A deterministic `IRandomSource` implementation (explicit algorithm, e.g. xoshiro256**, seeded per stream by hash(seed, name), reseeded per night by hash(seed, name, nightNumber)) that passes the CON-015 conformance suite cross-platform.

## Contracts

| Contract | Role |
|---|---|
| CON-015 | implements |
| CON-001 | consumes |

Consumed contracts are read-only. Implemented contracts must pass their conformance tests.

## File ownership (exclusive)

Only these paths may be created/modified by this ticket's session:

```
src/adapters/random/**
tests/adapters/random/**
```

## Acceptance criteria

- [x] CON-015 suite green via fixture subclass (determinism, independence, night reseeding, bounds, idempotence)
- [x] No System.Random (platform drift); algorithm documented in code
- [x] All conformance tests for implemented contracts pass (`dotnet test`, suites in `tests/contracts/`)
- [x] contract-compliance skill check passes (`.claude/skills/contract-compliance`)
- [x] Unit tests written first (superpowers **test-driven-development** skill) and passing
- [x] Ticket status + BOARD.md row updated on start and finish

## Implementation notes

Keep it dependency-free; the domains project must not reference this — it is injected at composition (CON-016).

## Session log

| Date | Event |
|---|---|
| 2026-07-13 | Session started; status TODO→IN PROGRESS. Blocker TKT-001 is DONE (wave 2). Read CON-015 (FROZEN), CON-001, `src/domains/kernel/Random.cs`, the abstract `RandomSourceConformanceTests` suite, and the build setup. |
| 2026-07-13 | TDD RED: wrote the CON-015 fixture subclass (`DeterministicRandomSourceConformanceTests`) + 13 adapter unit tests, incl. golden vectors generated by an **independent Python reference implementation** of xoshiro256**/SplitMix64/FNV-1a-64 (not by this C# code) — that is what makes CON-015's "stable across process runs and platforms" clause actually testable. Build → RED (CS0234, adapter type missing). |
| 2026-07-13 | TDD GREEN: added `src/adapters/random/` — `DeterministicRandomSource` (IRandomSource; per-name xoshiro256** streams, seed derived as `SplitMix64(SplitMix64(seed ^ fnv1a64(name)) ^ nightNumber)`), `Xoshiro256StarStar` (Lemire rejection sampling for unbiased `NextInt`; all-zero-state guard), `SplitMix64` (state expansion). First run: 61/64 — full CON-015 suite green, but the 3 golden-vector tests failed (my `Mix` avalanched `z + GOLDEN`; the reference spec avalanches `z`). Fixed the **code**, not the test → 64/64. |
| 2026-07-13 | Verified: `dotnet test TavernIdler.sln` 64 passed / 0 failed / 0 skipped (37 pre-existing kernel + 27 new random); Release build 0 warnings / 0 errors; `git diff` confirms `tests/contracts/` and `docs/contracts/` byte-identical. No `System.Random`, no `string.GetHashCode()` (per-process randomised) anywhere in the adapter. |
| 2026-07-13 | **File-ownership gap raised with user (approved exception).** Nothing in the repo compiles `src/adapters/**`: `TavernIdler.sln` has only Domains/App/Tests, and `TavernIdler.Tests` references Domains only. The CON-015 fixture subclass in `tests/adapters/random/` is auto-globbed into the Tests project (no csproj change needed), but the adapter assembly has no home. Per TKT-022's precedent (each adapter carries its own csproj) this ticket creates `src/adapters/random/TavernIdler.Adapters.Random.csproj` (inside ownership) and — with explicit user approval — makes two purely additive edits outside the block: register the project in `TavernIdler.sln` and add a `ProjectReference` to it from `tests/TavernIdler.Tests.csproj` (both owned by TKT-001, DONE; no wave-2 ticket touches them, so no concurrent-write risk). **Proposed follow-up ticket:** amend the File Ownership blocks of TKT-019/020/021, which will hit the identical wall. |
| 2026-07-13 | contract-compliance run — COMPLIANT; report below. Status IN PROGRESS→DONE. |

### Contract-compliance report

```
CONTRACT COMPLIANCE — TKT-017 — 2026-07-13
[PASS] 1. Ownership — changed files: src/adapters/random/** (4 new), tests/adapters/random/**
        (2 new), docs/tickets/{TKT-017,BOARD}.md. Two files outside the block —
        TavernIdler.sln and tests/TavernIdler.Tests.csproj — were changed under an
        EXPLICIT USER-APPROVED EXCEPTION (see session log): purely additive (one project
        entry, one ProjectReference), required because no project in the repo compiled
        src/adapters/**. Both are owned by TKT-001 (DONE); no wave-2 ticket touches them.
        Nothing in tests/contracts/ or any contract-generated interface file was modified.
[PASS] 2. Frozen-doc integrity — `git diff -- docs/contracts/` is empty. No CON-*.md and no
        REGISTRY.md row touched. CON-015 and CON-001 were Read only.
[PASS] 3. Interface fidelity —
        CON-015: DeterministicRandomSource implements IRandomSource with exactly
                 `IRandom GetStream(string name)` and `long Seed { get; }`; the internal
                 stream type implements IRandom with exactly `double NextDouble()` and
                 `int NextInt(int maxExclusive)`. The port interface itself
                 (src/domains/kernel/Random.cs) is unmodified.
                 Extra public members on the CONCRETE adapter — `BeginNight(int)` and
                 `NightNumber` — are not signature drift: CON-015 §Semantics mandates them
                 ("implementers must reseed streams at BeginService/BeginNight with the night
                 number"), and the frozen conformance suite's own `CreateSut(seed, nightNumber)`
                 factory presupposes seed+night construction. The port's surface is unchanged;
                 consumers still see only GetStream/Seed.
[PASS] 4. Conformance tests — CON-015: tests/contracts/random via fixture subclass —
        27 passed, 0 failed, 0 skipped (13 conformance + 14 adapter unit).
        Suite files byte-identical to pre-session state (verified by `git diff --stat`).
        Full solution: 64 passed / 0 failed / 0 skipped. Release build: 0 warnings.
[PASS] 5. Consumption fidelity — consumes CON-001 (kernel) only, and only for the IRandomSource/
        IRandom interfaces it implements. No kernel error modes are defined for this port; the
        one documented error mode of the port itself (NextInt(max < 1) →
        ArgumentOutOfRangeException) is implemented and covered by the frozen suite.
[PASS] 6. Domain purity — grep of src/domains for Godot/Unity usings: none. The adapter lives
        under src/adapters/ and is referenced by nothing in src/domains/ (Domains.csproj has no
        reference to it) — it is injected at composition per CON-016.
[N/A ] 7. Registry sync — not a contract-definition or contract-change ticket; REGISTRY.md
        untouched. Its CON-015 row already points at tests/contracts/random, which now executes.
VERDICT: COMPLIANT
```

> **2026-07-13 follow-up (TKT-027):** the explicit `Adapters.Random` ProjectReference and the `.sln` entry added here are superseded by the glob auto-discovery in TKT-027 (which removes the explicit ref to avoid a duplicate ProjectReference). Status here unchanged.
