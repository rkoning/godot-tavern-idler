# ── .NET build output ───────────────────────────────────────
bin/
obj/
[Dd]ebug/
[Rr]elease/
*.user
*.suo
.vs/

# ── Test / coverage artifacts ───────────────────────────────
[Tt]est[Rr]esults/
coverage*.json
coverage*.xml
*.trx

# ── /dispatch-wave session logs (per-ticket headless output) ─
.dispatch/

# ── Godot 4 (.NET) — populated from TKT-022 onward ──────────
# Godot 4+ engine cache / generated project files
.godot/
/android/

# Godot import cache (Godot 3 style; harmless to keep for safety)
.import/
*.import

# Export artifacts
export.cfg
export_presets.cfg
*.pck

# Auto-generated translations (from CSV)
*.translation

# Godot/Mono (.NET) build + crash artifacts
.mono/
data_*/
mono_crash.*.json
